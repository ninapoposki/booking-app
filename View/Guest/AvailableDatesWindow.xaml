<Window x:Class="BookingApp.View.Guest.AvailableDatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View.Guest"
        mc:Ignorable="d"
        Title="AvailableDatesWindow" Height="450" Width="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Dates}" AutoGenerateColumns="False" CanUserAddRows="False" Width="500" Height="300">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Initial Date" Binding="{Binding  InitialDate}" Width="*" />
                <DataGridTextColumn Header="End Date" Binding="{Binding   EndDate}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20" VerticalAlignment="Bottom">
            <Button Content="Book" Margin="5" Padding="10,0" Click="BookAccommodationClick"  Width="80" Height="20" />
            <Button Content="Cancel" Margin="5" Padding="10,0" Click="CancelClick" Width="80"  Height="20"/>
        </StackPanel>
    </Grid>
</Window>
