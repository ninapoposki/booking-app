<Window x:Class="BookingApp.View.Owner.PictureBrowseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View.Owner"
        mc:Ignorable="d"
        Title="PictureBrowseWindow" Height="450" Width="500">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="40">
            <TextBlock Text="Choose a picture"  Margin="0,10,0,0" TextWrapping="Wrap" HorizontalAlignment="Center" Height="20" Width="260"/>
            <ListBox x:Name="listBoxSlike" Height="250" Margin="0,10,0,0" Width="300" ItemsSource="{Binding Images, UpdateSourceTrigger=PropertyChanged}"
                     SelectedItem="{Binding selectedImage, UpdateSourceTrigger=PropertyChanged}"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Path}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Confirm" Margin="0,0,0,0" Width="100" Click="ConfirmClick" Height="18"/>
        </StackPanel>
    </Grid>
</Window>