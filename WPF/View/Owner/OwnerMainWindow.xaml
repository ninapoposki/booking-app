<Window x:Class="BookingApp.WPF.View.Owner.OwnerMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.View.Owner"
        mc:Ignorable="d"
        Title="OwnerMainWindow" Height="500" Width="1000">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="100" />
            <RowDefinition Height="100" />
            
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding OwnerDTO.FirstName}" Margin="10,10,0,10"/>
            <TextBlock Text="{Binding OwnerDTO.LastName}" Margin="5,10"/>
        </StackPanel>

       
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="22,20,0,20">
            <Button Content="Add Accommodation" Margin="5" Padding="10,0" Click="AddAccommodationClick" Width="150" />
            <Button Content="GradeGuest" Margin="5" Padding="10,0" Click="GradeGuestClick" Width="89" />
            <Button Content="Notifications" Margin="5" Padding="10,0" Click="NotificationsClick" Width="89" />
        </StackPanel>

        <DataGrid Grid.Row="2" Margin="10,0,10,-224" AutoGenerateColumns="False" x:Name="AccommodationDataGrid" ItemsSource="{Binding Accommodation, UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedAccommodation, UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single" IsReadOnly="True" >
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="FontWeight" Value="Regular"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="100"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location.City}" Width="100"/>
                <DataGridTextColumn Header="Accommodation Type" Binding="{Binding AccommodationType}" Width="130"/>
                <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" Width="100"/>
                <DataGridTextColumn Header="Min Stay Days" Binding="{Binding MinStayDays}" Width="100"/>
                <DataGridTextColumn Header="Cancellation Period" Binding="{Binding CancellationPeriod}" Width="120"/>
                <DataGridTemplateColumn Header="Images" Width="*">
                    
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Path}" Height="50"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
